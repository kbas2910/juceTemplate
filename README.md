template based on: 
https://github.com/JanWilczek/audio-plugin-template

How to/ steps:

1) make local clone of repository
    - in terminal: go to directory were repository is supposed to live
    - copy url of repository from github (click green button that says "code")
    - in terminal type: git clone url-from-repo

2) Change files to fit own project: 
    a. open top level CMakeLists.txt in editor
    
    b. inside the directory "plugin" adapt te file CMakeLists.txt to your needs
	- do this and that
	- do some more
